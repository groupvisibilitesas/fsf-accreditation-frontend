# URL de base de l'API NestJS (fsf-acreditation-backend), utilisée uniquement cote serveur
# (Route Handlers, Server Components) — jamais exposee au navigateur.
BACKEND_URL=http://localhost:3000/api/v1
